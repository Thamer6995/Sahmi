# تقرير الفحص التقني الشامل - مشروع سهمي (Sahmi)

**تاريخ التقرير:** 19 يوليو 2026
**آخر Commit مفحوص:** `4304bb8` (فرع `claude/sahmk-stock-analyzer-telegram-0yykdh`)
**منهجية التقرير:** كل بند هنا إما (أ) تم التحقق منه فعليًا بتشغيل أمر/قراءة كود/سجل حقيقي، أو (ب) مُعلَّم صراحة "لم يتم التحقق منه". لا توجد افتراضات غير موسومة.

---

## 1. Project Status (حالة المشروع)

| السؤال | الإجابة | الدليل |
|---|---|---|
| هل المشروع يعمل؟ | **جزئيًا.** محرك التقييم والواجهة يعملان فعليًا. تسليم التنبيهات عبر Telegram في ظروف حقيقية (سهم يستوفي الشروط فعليًا) **لم يُشاهَد ناجحًا ولو مرة واحدة** خلال كامل هذه الجلسة/السجل المتوفر. | انظر قسم 5 |
| هل Build ينجح؟ | **نعم**، للسيرفر والواجهة معًا. | `tsc --noEmit` (server) و`tsc -b && vite build` (frontend) - نُفِّذا فعليًا الآن، صفر أخطاء |
| هل يوجد TypeScript Errors؟ | **لا - صفر** في كلا المشروعين. | تشغيل فعلي: `server: 0 errors`, `frontend: 0 errors` |
| هل يوجد Runtime Errors؟ | **نعم، حقيقية ومسجَّلة فعليًا في سجلات الإنتاج (Render).** | راجع قسم 8 - خطأان مختلفان حدثا فعليًا خلال آخر 48 ساعة |
| هل يوجد Console Errors؟ | **لم يتم التحقق منه.** لم يُفتح المتصفح فعليًا لمراقبة الـ Console في هذه الجلسة. | - |
| هل يوجد Firebase Errors؟ | **نعم، مؤكَّدة فعليًا مرتين خلال يومين.** `RESOURCE_EXHAUSTED: Quota exceeded` على Firestore (خطة Spark، سقف 20K كتابة/يوم). | لقطة شاشة فعلية من Firebase Console: "100% of limit used ... exceeded by 287" (19 يوليو)، ونفس الخطأ بالضبط بتاريخ 18 يوليو |

---

## 2. Features Status (حالة الميزات)

**ملاحظة مهمة:** بعض الميزات أدناه ظننتُ سابقًا في هذه المحادثة أنها "غير موجودة وتحتاج بناء من الصفر" (عند سؤال المستخدم عن رأيي في تحسينات مقترحة) - **هذا كان خطأ مني**. الفحص الفعلي للكود الآن يُظهر أنها موجودة جزئيًا. تم تصحيح ذلك في الجدول أدناه.

| الميزة | مكتملة | تعمل فعليًا | جزئية | لا تعمل | السبب | نسبة الإنجاز |
|---|:---:|:---:|:---:|:---:|---|:---:|
| Investment Score (محرك التقييم) | ✅ | ✅ (مؤكَّد باختبارات فعلية على رموز حقيقية سابقًا في الجلسة) | | | 6 فحوصات جودة + 4 تقييم + 5 توزيعات + 5 فنية، مطابقة للأوزان 35/25/25/15 | 100% |
| استبعاد القطاع المالي | ✅ | ✅ (مؤكَّد فعليًا: سهم 1120 استُبعد بنجاح) | | | تم إصلاح الجذر (مصدر القطاع من `/company/{symbol}/` وليس `/companies/`) | 100% |
| Dashboard (لوحة التحكم) | | | ⚠️ | | تعمل، لكن مؤشر "استهلاك API اليوم" **معطّل الآن فعليًا** (يقرأ collection لم يعد أحد يكتب فيه بعد إصلاح اليوم - راجع قسم 8، خطأ #4) | 80% |
| Stock Screener (فلترة/بحث) | | | | | **موجودة فعليًا** بصفحة `Explorer.tsx` - بحث + فلاتر (قطاع/درجة/Yield/PE/ROE/D-E) + فرز بالنقر على الأعمدة | 90% (لا قوالب محفوظة) |
| Dividend Calendar (تقويم التوزيعات) | ✅ | ✅ | | | **موجودة فعليًا وكاملة** بصفحة `DividendCalendar.tsx`، بفلترة شهر/قطاع | 95% |
| Historical Investment Score (سجل تاريخي) | | | ⚠️ | | **موجود فعليًا لكن أساسي جدًا**: كل تقييم جديد يُضاف تلقائيًا لـ subcollection `scores/{symbol}/history`، ويُعرض في صفحة السهم كقائمة نصية (تاريخ + رقم) - لا يوجد رسم بياني | 50% |
| Score Change Log (سجل تغيّر التقييم) | | | ⚠️ | | نفس آلية البند أعلاه بالضبط - نفس البيانات، بدون تفاصيل "أي فحص تغيّر ولماذا" | 40% |
| Confidence Score (درجة الثقة) | | | ⚠️ | | حقل `dataCompleteness` موجود ومحسوب فعليًا، ويُعرض كعمود "اكتمال البيانات" في Explorer وصفحة السهم - لكن غير مُسمّى/مُبرز كـ"درجة ثقة" مستقلة | 60% |
| Risk Score (درجة المخاطرة) | | | | ✅ | **غير موجودة إطلاقًا** - لا كود، لا واجهة، لا حتى إشارة في التصميم | 0% |
| AI Summary (ملخص AI) | | | | ✅ | **غير موجودة إطلاقًا** | 0% |
| Dividend Ranking / Top Lists | | | ⚠️ | | يمكن الوصول لنفس النتيجة عبر فرز عمود "عائد التوزيعات" في Explorer، لكن لا توجد صفحة/عرض مخصص باسمها | 40% (عبر Explorer فقط) |
| Multi-Filter Presets (قوالب فلاتر محفوظة) | | | | ✅ | فلاتر Explorer تُفقَد عند تحديث الصفحة (state محلي فقط، غير محفوظ) | 0% |
| Event Log (سجل أحداث) | | | ⚠️ | | بيانات موجودة فعليًا في Firestore (`syncJobs` - تقدّم كل دفعة معالجة)، **لكن لا توجد أي واجهة تعرضها** - لم يُستهلَك في الواجهة إطلاقًا (تأكَّد بالبحث: صفر نتائج) | 20% (بيانات فقط، بدون واجهة) |
| Opportunity History (سجل الفرص التاريخي) | | | | ✅ | غير موجودة | 0% |
| Alert Levels (مستويات التنبيه) | ✅ | ✅ | | | 3 مستويات (70-79 داخل التطبيق فقط، 80-89، 90-100) - **مؤكَّد فعليًا بالكود والاختبارات** | 100% |
| Scheduled Reports (تقارير مجدولة/ملخص دوري) | | | | ✅ | غير موجودة - التنبيهات الفورية فقط عند استيفاء الشروط | 0% |
| Custom Scoring Weights (أوزان مخصصة) | ✅ | ✅ | | | **موجودة فعليًا وكاملة** - `settings.scoringWeights` مربوطة بواجهة `Settings.tsx` والباك إند | 100% |
| Export Excel / PDF | | | | ✅ | **غير موجودة إطلاقًا** - صفر نتائج بحث عن "export/xlsx/csv/pdf" في كامل الواجهة | 0% |
| Telegram Alerts (تنبيهات Telegram) | | | ⚠️ | | الكود موجود وسليم بنيويًا، لكن **لم يُؤكَّد نجاح إرسال فعلي حقيقي** ناتج عن سهم مستوفٍ للشروط - راجع قسم 5 بالتفصيل | غير معروف (الكود 100%، النتيجة الفعلية 0% مؤكَّدة) |

---

## 3. Firebase Audit

### Collections الموجودة فعليًا (من الكود، تأكيد عبر grep شامل)

| Collection | من يكتب فيها | من يقرأها | ملاحظة |
|---|---|---|---|
| `companies` | السيرفر (Admin SDK) | الواجهة + السيرفر | |
| `quotes` | السيرفر | الواجهة + السيرفر | |
| `historicalPrices` | السيرفر | السيرفر فقط (المخطط: `{symbol}_{date}` doc) | لا تُعرض مباشرة، تُستخدم لحساب المؤشرات الفنية |
| `financials` | السيرفر | السيرفر فقط | |
| `ratios` | السيرفر | الواجهة + السيرفر | |
| `dividends` | السيرفر | الواجهة + السيرفر | |
| `scores` (+ `scores/{symbol}/history`) | السيرفر | الواجهة + السيرفر | الـ subcollection تُعرض جزئيًا (قسم 2) |
| `alerts` | السيرفر | الواجهة | |
| `watchlist` | **العميل مباشرة** (لا يوجد route خادم لها) | الواجهة | القاعدة تسمح بكتابة مباشرة من المستخدم المصادَق |
| `settings` | السيرفر (عبر `/updateSettings`) | الواجهة + السيرفر | |
| `apiUsage` | **لا أحد يكتب فيها بعد اليوم** (كانت تُكتب، أُوقفت في commit `ed38bae`) | الواجهة (Dashboard) - قراءة معطوبة فعليًا الآن | **معطوبة - راجع خطأ #4** |
| `syncJobs` | السيرفر (`batchRunner.ts`) | لا أحد (صفر استهلاك في الواجهة) | بيانات ميتة عمليًا |
| `_scheduleState` | السيرفر | السيرفر فقط | حالة الجدولة (hasRunToday, chunk progress) |
| `_rateLimits` | السيرفر | السيرفر فقط | |
| `_devRawResponses` | السيرفر (DEV_MODE فقط) | السيرفر (route محمي، DEV_MODE فقط) | |

**العدد الإجمالي: 15 مجموعة رئيسية + subcollection واحدة (`scores/*/history`).**

### Security Rules (`firestore.rules`)

- موجودة، مفحوصة فعليًا (95 سطر).
- **التعليق التوضيحي في أعلى الملف قديم/غير دقيق**: يذكر "الكتابة الفعلية تتم من Cloud Functions" - لكن المشروع انتقل فعليًا لسيرفر Render (Express) منذ commit `6d0757f`. القواعد نفسها لا تزال صحيحة وظيفيًا (لأن Admin SDK يتجاوز القواعد بغض النظر عن مصدره)، لكن التوثيق مضلِّل لأي مطوّر يقرأه لاحقًا.
- `apiUsage` له قاعدة قراءة صريحة رغم أن الكتابة إليه توقفت فعليًا اليوم - قاعدة يتيمة.
- `watchlist` هو الوحيد المسموح بكتابة مباشرة من العميل - غير محمي بأي route خادم للتحقق.

### Indexes (`firestore.indexes.json`)

- ملف موجود (1888 بايت). لم يُفحص محتواه التفصيلي مقابل الاستعلامات الفعلية في هذا التقرير (لم يتم التحقق من التطابق الكامل بين كل استعلام `.where()` مركّب في الكود وبين الفهارس المعرَّفة - يحتاج فحصًا منفصلًا لو ظهرت أخطاء "index required" فعلية في الإنتاج، وهذا لم يحدث في السجلات المتوفرة).

### Cloud Functions / Scheduled Functions

- **لا توجد أي Cloud Functions في المشروع حاليًا - صفر.** تم الانتقال الكامل لـ Render + GitHub Actions (commit `6d0757f`). لا يوجد مجلد `functions/` في المستودع.
- الجدولة بالكامل تعتمد على GitHub Actions cron (وليس Firebase Scheduled Functions) - راجع قسم 6.

### Firebase Secrets / بيئة التشغيل

- المفاتيح الحساسة (`FIREBASE_SERVICE_ACCOUNT_BASE64`, `SAHMK_API_KEY`, `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `CRON_SECRET`) مُعرَّفة في `render.yaml` كمتغيرات بيئة `sync: false` (تُدخَل يدويًا في Render، غير مخزَّنة في الكود). هذا تصميم سليم.
- **لم يتم التحقق من القيم الفعلية الحية على Render** في هذه الجلسة (لا صلاحية وصول مباشر لبيئة Render من هنا) - القيم الموجودة "من حيث المبدأ" بناءً على أنشطة سابقة في هذه المحادثة (تم إعدادها يدويًا)، لكن لم تُفحص الآن حرفيًا واحدة واحدة.
- `render.yaml` يُظهر `plan: free` لخدمة الويب على Render نفسها (منفصل تمامًا عن خطة Firebase) - هذا يفسّر سلوك "spin down after inactivity" المُشاهَد فعليًا في لوحة Render.
- `DEV_MODE` مُعرَّف بقيمة `"false"` في `render.yaml`، لكن **لم يتم التحقق من القيمة الفعلية الحالية على Render مباشرة** (قد تكون عُدِّلت يدويًا أثناء الاختبار المكثّف في الجلسات السابقة ولم تُعَد لـ false فعليًا - هذه المهمة كانت مُدرَجة كمهمة متبقية في وقت سابق ولم يُؤكَّد إغلاقها).

---

## 4. SAHMK Audit

| السؤال | الإجابة |
|---|---|
| هل الاتصال يعمل؟ | **نعم، مؤكَّد فعليًا** - طلبات حقيقية نجحت خلال الجلسات السابقة (شركات فعلية، أسعار، بيانات مالية حقيقية جُلبت وخُزِّنت). |
| هل يتم جلب البيانات؟ | **نعم** - `/companies/`, `/company/{symbol}/`, `/quotes/`, `/historical/{symbol}/`, `/financials/{symbol}/`, `/analytics/ratios/{symbol}/`, `/dividends/{symbol}/` - كلها مُستخدَمة فعليًا في الكود (`SahmkService.ts`). |
| كم Endpoint مستخدم؟ | **7 endpoints** حقيقية موثَّقة ومستخدَمة، لا يوجد أي endpoint "مُخترَع" غير موثَّق (يطابق شرط المواصفة الأصلي بعدم تلفيق endpoints). |
| هل يوجد Rate Limit؟ | **نعم، حقيقي ومؤكَّد** - SAHMK يرجع 429 مع رسالة `"Expected available in N seconds"`. الكود يحترم هذه المهلة فعليًا (`httpClient.ts`، حتى 6 محاولات، حتى 35 ثانية انتظار لكل محاولة). |
| هل توجد أخطاء؟ | لا يوجد سقف يومي فعلي من SAHMK نفسها (مؤكَّد سابقًا بالاختبار: تجاوزنا 20,000 طلب في يوم واحد أثناء الاختبار المكثّف دون أي رفض بسبب "حد يومي" - كل ما ظهر كان throttling مؤقت بالثانية). |
| ما البيانات غير المتوفرة في خطة Starter؟ | endpoint الجملة `/companies/` **لا يرجع** `sector_name`/`sector_name_ar`/`market_id` إطلاقًا (مؤكَّد من raw response فعلي) - تم حلها بجلب `/company/{symbol}/` المفرد لكل رمز أثناء تحديث القوائم المالية. P/E وP/B **غير متوفرين إطلاقًا** في `/analytics/ratios/` - مصدرهما الوحيد `fundamentals` ضمن `/company/{symbol}/`. |

---

## 5. Telegram Audit (تفصيلي - بدون كلمات احتمالية)

### مسار الكود الكامل (مؤكَّد بقراءة الكود سطرًا سطرًا الآن):

1. **`server/src/alerts/evaluateAlert.ts`** - دالة `evaluateAndMaybeSendAlert()`. تُطبَّق 9 شروط بالترتيب (درجة ≥70، ليست 70-79، درجة ≥ الحد الأدنى بالإعدادات، اكتمال بيانات ≥70%، ليست خاسرة، بدون تحذير حرج، إشارة جديدة/جوهرية، Telegram مفعّل، بدون تكرار خلال فترة التهدئة). **أي شرط يفشل يوقف العملية فورًا قبل الوصول لـ Telegram - هذا سلوك مصمَّم ومقصود، وليس عطلاً.**
2. عند اجتياز كل الشروط: `buildAlertMessage()` في `server/src/services/telegram/messageBuilder.ts` يبني نص الرسالة.
3. `telegramService.sendMessage()` في `server/src/services/telegram/TelegramService.ts` (سطر 30-94) يرسل طلب HTTP فعلي إلى `https://api.telegram.org/bot{TOKEN}/sendMessage`.
4. عند النجاح: `createAlert()` في `alertsRepo.ts` يحفظ السجل بـ `sentToTelegram: true`.

### الإجابات المباشرة:

| السؤال | الجواب المحدَّد |
|---|---|
| هل Bot Token موجود؟ | الكود يقرأه من `TELEGRAM_BOT_TOKEN` عبر `config/env.ts` (دالة `required()` - تفشل بخطأ صريح لو غير موجود). **لم يتم التحقق من القيمة الفعلية على Render الآن مباشرة في هذه الجلسة.** |
| هل Chat ID موجود؟ | نفس الشيء - `TELEGRAM_CHAT_ID`. **لم يتم التحقق من القيمة الفعلية على Render الآن مباشرة.** |
| هل Firebase Function تستدعي Telegram؟ | **لا يوجد Firebase Function إطلاقًا في المشروع الحالي** (تم الانتقال لـ Render). الاستدعاء الفعلي من `server/src/scheduled/dailyPriceAndAlertScan.ts` → `scanAndAlertAllSymbols()` → `runAlertCheckForSymbol()` → `evaluateAndMaybeSendAlert()`. |
| هل يتم إنشاء التنبيه؟ | **نعم للمستويين 70-79 و80+** - `createAlert()` يُستدعى فعليًا لكل تقييم يتجاوز 70، بغض النظر عن إرسال Telegram من عدمه. |
| هل يتم استدعاء Telegram API؟ | **نعم، لكن فقط عند اجتياز كل الشروط التسعة** - لم يحدث هذا فعليًا مع أي رمز حقيقي في السجل المتوفر لهذه الجلسة (كل الاختبارات السابقة أظهرت `sent: false` بسبب "الدرجة الكلية أقل من 70"). |
| هل Telegram API يرجع Success؟ | **لم يتم التحقق منه لسهم حقيقي مستوفٍ للشروط.** الدليل الوحيد المتوفر على أن API نفسه يستجيب هو زر "اختبار الآن" في Dashboard (`testTelegramConnection` → `sendTestMessage()`) - **ولا يوجد في سجل هذه المحادثة أي تأكيد بأن هذا الزر ضُغِط فعليًا ونجح.** |
| هل تم اختبار إرسال رسالة فعلية؟ | **لا - لم يتم التحقق من هذا فعليًا في أي نقطة من هذه الجلسة أو الجلسات السابقة المسجَّلة.** لا توجد لقطة شاشة، لا سجل، لا تأكيد نصي من المستخدم بوصول رسالة Telegram حقيقية. |
| إذا لم تصل الرسالة، في أي خطوة تتوقف العملية؟ | **آخر خطوة مؤكَّدة أنها تعمل فعليًا:** حساب الدرجة الكلية (`computeScoreForSymbol`) وتقييم الشروط التسعة في `evaluateAndMaybeSendAlert` - كلاهما مختبَر وناجح فعليًا لرموز حقيقية (2222، 2280، 7010، 1211 - نجحت دون استبعاد، 1120 استُبعد صح). **أول خطوة غير مؤكَّدة فعليًا:** استدعاء `telegramService.sendMessage()` الفعلي لسهم حقيقي مستوفٍ للشروط - لم يحدث هذا قط في السجل المتوفر (لأن لا سهم وصل فعليًا لدرجة ≥80 مع كل الشروط الأخرى مجتمعة في وقت واحد أثناء الاختبار). |

### خلاصة قسم Telegram

**الكود سليم بنيويًا وصحيح منطقيًا (مؤكَّد بالقراءة والاختبارات الوحدوية لـ `messageBuilder.test.ts`).** لكن **لا يوجد دليل واحد في تاريخ هذا المشروع على وصول رسالة Telegram حقيقية ناتجة عن تقييم سهم فعلي يستوفي كل الشروط.** هذه فجوة حرجة بين "الكود يبدو صحيحًا" و"النظام أثبت أنه يعمل فعليًا".

---

## 6. Scheduled Jobs

| اسم الوظيفة | الغرض | هل تعمل؟ | آخر تشغيل مؤكَّد | النتيجة | الخطأ إن وجد |
|---|---|:---:|---|---|---|
| `daily-price-and-alert-scan` | أسعار + بيانات تاريخية + تقييم + تنبيهات (يوميًا) | **غير مستقرة** | Run #24، 19 يوليو 13:57-14:59 (62 دقيقة) | **فشل** | علِق بحالة `busy:true` لمدة ساعة كاملة متواصلة بدون أي تقدّم - **قفل التزامن (jobLock) لا يُحرَّر عند تعليق العملية الأصلية لفترة طويلة (bug حقيقي غير مُصلَح، مكتشَف اليوم)** |
| `weekly-financials-scan` | القوائم المالية والنسب (أسبوعيًا) | **غير مستقرة** | Run #24، 19 يوليو 13:56-14:51 (54 دقيقة) | **فشل** | نفس السبب أعلاه + تأكَّد أيضًا فعليًا: استنزاف حصة كتابة Firestore اليومية (100% مستخدَمة، تجاوز 287) |
| `daily-dividends-scan` | التوزيعات (يوميًا) | **غير مستقرة** | Run #24، 19 يوليو 13:57-15:01 (65 دقيقة) | **فشل** | نفس الأسباب أعلاه |

**سجل حقيقي إضافي (آخر 24 تشغيلة مرصودة فعليًا عبر GitHub Actions API):**
- 24 تشغيلة إجمالًا منذ 16 يوليو.
- **10 تشغيلات فاشلة من أصل 24 (~42%)** - أسباب متعددة موثَّقة فعليًا: أخطاء اتصال curl (مُصلَحة)، استنزاف حصة Firestore (مرتين، مُصلَح جزئيًا)، انقطاع صامت لجدولة GitHub Actions لساعات متعددة بدون سبب واضح من طرفنا (**غير قابل للإصلاح من الكود - مشكلة منصة GitHub نفسها**، لوحظت 3 مرات على الأقل خلال يومين)، وخلل قفل التزامن الجديد (اليوم، غير مُصلَح).
- **لا يوجد دليل واحد على اكتمال دورة تنبيهات كاملة بنجاح 100% (يعني: أسعار + بيانات تاريخية + تقييم + تنبيهات لكل السوق، بدون توقف/فشل جزئي) في آخر 3 أيام على الأقل.**

**بديل الجدولة:** GitHub Actions cron (وليس Firebase Scheduled Functions، بسبب قيد فوترة Blaze في السعودية). هذا البديل نفسه أثبت عدم موثوقية متكررة (تخطي تشغيلات مجدولة بصمت دون أي تنبيه أو سجل خطأ - موثَّق 3 مرات فعليًا خلال 16-19 يوليو).

---

## 7. Firestore Audit - ما يُحفَظ فعليًا

| البيانات | تُحفَظ؟ | التفاصيل |
|---|:---:|---|
| Stocks (الشركات) | ✅ | `companies` - رمز، اسم عربي/إنجليزي، قطاع، صناعة، سوق |
| Scores (التقييمات) | ✅ | `scores/{symbol}` - أحدث تقييم فقط (استبدال كامل عند كل حساب جديد) |
| Historical Snapshots (لقطات تاريخية للتقييم) | ⚠️ جزئي | `scores/{symbol}/history` - لقطة (الدرجة + اكتمال البيانات + الوقت) فقط، بدون تفاصيل الفحوصات الفردية |
| Alerts (التنبيهات) | ✅ | `alerts` - لكل تقييم يتجاوز 70، مع حالة الإرسال لـTelegram |
| Events (سجل أحداث عام) | ⚠️ جزئي | `syncJobs` يُسجَّل فعليًا (تقدّم كل دفعة معالجة، نجاح/فشل)، **لكن لا واجهة تعرضه - بيانات ميتة عمليًا من منظور المستخدم** |
| Reports (تقارير) | ❌ | غير موجودة إطلاقًا |
| Feed (تغذية إخبارية/فرص) | ❌ | غير موجودة إطلاقًا |
| Timeline (خط زمني موحّد لكل الأحداث) | ❌ | غير موجود كمفهوم موحّد - البيانات مبعثرة بين `alerts` و`scores/*/history` و`syncJobs` بدون أي طبقة توحّدها |
| Quotes (الأسعار) | ✅ | `quotes/{symbol}` |
| Historical Prices (OHLCV) | ✅ | `historicalPrices/{symbol}_{date}` |
| Financials (القوائم المالية) | ✅ | `financials/{symbol}_{periodType}_{period}` |
| Ratios (النسب) | ✅ | `ratios/{symbol}` |
| Dividends (التوزيعات) | ✅ | `dividends/{symbol}_{date}` |
| Settings (الإعدادات) | ✅ | `settings/app` |
| Watchlist (قائمة المراقبة) | ✅ | `watchlist/{symbol}` - كتابة مباشرة من العميل |

---

## 8. Error List (مرتّبة من الأخطر إلى الأقل)

### #1 - قفل التزامن (jobLock) قد يعلق للأبد بدون تحرير [Critical]
- **الوصف:** أضفنا اليوم قفلًا بالذاكرة (`server/src/utils/jobLock.ts`) لمنع تشغيل نفس المهمة المجدولة بالتزامن. لكن إن علّقت العملية الأصلية داخل القفل (بسبب بطء SAHMK أو Firestore) لفترة طويلة، **يبقى القفل محجوزًا دون أي مهلة قصوى (timeout)**، فترجع كل الطلبات اللاحقة `busy:true` إلى ما لا نهاية.
- **السبب:** لا يوجد hard timeout يحرِّر القفل قسرًا.
- **الملفات المتأثرة:** `server/src/utils/jobLock.ts`, `server/src/routes/internal.ts`.
- **تأثيره:** **مؤكَّد فعليًا اليوم** - `daily-price-and-alert-scan` بقي `busy:true` لمدة **ساعة كاملة متواصلة** (13:57-14:59) دون أي تقدّم، حتى استسلمت حلقة GitHub Actions بعد استنفاد ميزانيتها.
- **خطر إضافي غير مؤكَّد:** بما أن القفل بالذاكرة (لا يُصفَّر إلا بإعادة تشغيل عملية Node على Render)، **من المحتمل أن يكون لا يزال محجوزًا الآن وقت كتابة هذا التقرير**، مما يعني تشغيلة الغد المجدولة (12:30) قد تفشل فورًا بـ`busy:true` أيضًا **إلا إذا** أعاد Render تشغيل الخدمة تلقائيًا بسبب "spin down" الخمول قبل ذلك. **لم يتم التحقق من هذا الاحتمال فعليًا.**
- **الأولوية:** Critical.

### #2 - استنزاف حصة كتابة Firestore اليومية (خطة Spark) [Critical]
- **الوصف:** حصة Firestore المجانية (20,000 كتابة/يوم) استُنزفت بالكامل **مرتين خلال يومين متتاليين** (18 و19 يوليو)، مؤكَّد بلقطات شاشة فعلية من Firebase Console تُظهر "100% ... exceeded".
- **السبب:** أي دورة اختبار/تشخيص كاملة (تشغيل يدوي واحد لكل الوظائف الثلاث) تستهلك آلاف الكتابات الحقيقية لبيانات ~500 شركة. المشروع لا يزال على خطة Spark المجانية (المستخدم اختار عدم الترقية لـBlaze بناءً على تقدير أن الاستخدام اليومي العادي لن يصل للسقف - وهذا التقدير صحيح للاستخدام العادي، لكن أي إعادة اختبار يدوية تكسر هذا الافتراض).
- **الملفات المتأثرة:** لا يوجد ملف واحد - هذا قيد بنيوي على مستوى المشروع/الخطة.
- **تأثيره:** يمنع أي عملية كتابة إضافية حتى تصفير الحصة (~10 صباحًا بتوقيت الرياض يوميًا).
- **الأولوية:** Critical (متكرر، ويعطّل التشغيل بالكامل كل مرة يحدث).

### #3 - عدم موثوقية جدولة GitHub Actions (تخطي تشغيلات صامت) [Critical]
- **الوصف:** لوحظ فعليًا (مؤكَّد بسجلات GitHub Actions API) أن الجدولة التلقائية (`cron: '3,33 * * * *'` - كل 30 دقيقة) تتوقف عن العمل بصمت تام لساعات متعددة متتالية (لوحظ فعليًا: فجوة 4+ ساعات، وفجوة 3+ ساعات في مناسبات منفصلة خلال 18-19 يوليو)، دون أي رسالة خطأ أو سجل فشل - التشغيلة ببساطة لا تُطلَق إطلاقًا.
- **السبب:** سلوك منصة GitHub Actions نفسها (معروف أنه يمكن أن يتأخر/يُسقَط تحت ظروف حمل عالٍ) - **خارج نطاق كود المشروع تمامًا، لا يوجد إصلاح كود ممكن.**
- **الملفات المتأثرة:** `.github/workflows/scheduled-scans.yml` (لا يوجد خطأ فيه، المشكلة في منصة GitHub).
- **تأثيره:** التنبيه اليومي المتوقع الساعة 12:30 لم يصل فعليًا في يوم 18 يوليو ويوم 19 يوليو بسبب هذا بالضبط (بالإضافة لأسباب أخرى تراكمت).
- **الأولوية:** Critical (يهدد الوعد الأساسي للمشروع: "تنبيه في وقته").

### #4 - مؤشر "استهلاك API اليوم" في Dashboard معطوب فعليًا [High]
- **الوصف:** بعد إصلاح اليوم لنقل عداد استخدام SAHMK من Firestore للذاكرة (commit `ed38bae`)، توقف الكتابة لـ collection `apiUsage` نهائيًا. لكن `Dashboard.tsx` (سطر 25، 35-38) لا يزال يقرأ من نفس الـ collection عبر `onSnapshot` ويعرضه كـ`{usageCount} / 5000`.
- **السبب:** تعديل الباك إند لم يُرافَق بتحديث الواجهة المعتمِدة على نفس البيانات.
- **الملفات المتأثرة:** `frontend/src/pages/Dashboard.tsx`، `server/src/services/sahmk/rateTracker.ts`.
- **تأثيره:** المؤشر سيبقى مجمّدًا على آخر قيمة قبل الإصلاح (أو صفر) للأبد - معلومة مضلِّلة معروضة للمستخدم، ورقم "5000" نفسه أصلًا كان قديمًا (السقف الفعلي رُفع لـ20,000 قبل هذا).
- **الأولوية:** High (يعرض معلومة خاطئة بثقة للمستخدم، لكن لا يعطّل أي وظيفة جوهرية).

### #5 - عدم التحقق الفعلي من نجاح إرسال Telegram على الإطلاق [High]
- **الوصف:** راجع قسم 5 بالتفصيل - لا يوجد دليل واحد في تاريخ المشروع على نجاح إرسال تنبيه Telegram حقيقي.
- **السبب:** لم يحدث بعد أن استوفى سهم حقيقي كل الشروط التسعة في وقت واحد أثناء الاختبار.
- **الملفات المتأثرة:** لا يوجد خطأ كودي محدَّد - هذه فجوة في التحقق (verification)، وليست بالضرورة عطلاً.
- **تأثيره:** لا يوجد ضمان فعلي أن المسار الكامل (من التقييم إلى وصول الرسالة الفعلية على تطبيق Telegram) يعمل حتى لو كان كل جزء على حدة يبدو سليمًا.
- **الأولوية:** High.

### #6 - القواعد الأمنية (`firestore.rules`) تحتوي توثيقًا قديمًا مضلِّلًا [Medium]
- **الوصف:** التعليق العلوي يذكر "Cloud Functions" كمصدر الكتابة، بينما المشروع انتقل لـRender منذ عدة commits.
- **السبب:** لم يُحدَّث التوثيق عند الانتقال المعماري.
- **الملفات المتأثرة:** `firestore.rules`.
- **تأثيره:** لا تأثير وظيفي حاليًا (القواعد نفسها صحيحة)، لكن يضلِّل أي مراجعة مستقبلية.
- **الأولوية:** Medium.

### #7 - `syncJobs` بيانات تُكتَب لكن لا تُستهلَك [Medium]
- **الوصف:** كل تشغيلة مجدولة تكتب تقدّمها لـ`syncJobs`، لكن لا توجد أي صفحة/مكوّن في الواجهة يقرأها.
- **السبب:** لم تُبنَ واجهة "سجل الأحداث/المهام" رغم توفّر البيانات الخام لها بالكامل.
- **الملفات المتأثرة:** `server/src/utils/batchRunner.ts` (الكتابة)، لا يوجد ملف واجهة (القراءة غير موجودة).
- **تأثيره:** فرصة ضائعة - كان يمكن استخدام هذه البيانات لتشخيص مشاكل مثل الأخطاء #1-#3 أعلاه مباشرة من لوحة التحكم بدل الرجوع لسجلات GitHub Actions/Render يدويًا.
- **الأولوية:** Medium.

### #8 - `watchlist` بدون تحقق من صحة المدخلات عبر الخادم [Low]
- **الوصف:** القاعدة الأمنية تسمح بكتابة مباشرة من العميل بدون المرور بـroute خادم يتحقق من صحة البيانات (على عكس بقية الـcollections).
- **السبب:** قرار تصميم مقصود (مذكور في تعليق القاعدة نفسها: "التطبيق شخصي بحساب واحد").
- **الملفات المتأثرة:** `firestore.rules`.
- **تأثيره:** منخفض فعليًا لأن المشروع شخصي بمستخدم واحد فقط - لكن يبقى نقطة ضعف نظرية لو تغيّر نطاق الاستخدام مستقبلًا.
- **الأولوية:** Low.

### #9 - حزمة الواجهة الأمامية أكبر من 500KB بدون تقسيم [Low]
- **الوصف:** تحذير فعلي من `vite build`: `dist/assets/index-*.js 667.67 kB`.
- **السبب:** لا يوجد `manualChunks`/`dynamic import()` في إعداد Vite.
- **الملفات المتأثرة:** `frontend/vite.config.ts`.
- **تأثيره:** بطء تحميل أولي طفيف - غير حرج لتطبيق شخصي بمستخدم واحد.
- **الأولوية:** Low.

---

## 9. Technical Debt

- **كود مكرر:** لم يُعثَر على تكرار واضح بين الملفات (البنية معيارية - repo/job/scoring/route منفصلة بوضوح).
- **ملفات كبيرة جدًا:** لا يوجد ملف يتجاوز 450 سطرًا (`DevTest.tsx` هو الأكبر بـ447 سطرًا) - ضمن الحدود المعقولة.
- **Services غير مستخدَمة:** لم يُعثَر على أي service كامل غير مُستخدَم.
- **Components غير مستخدَمة:** لم يُعثَر على أي.
- **Dead Code (كود ميت مؤكَّد):**
  - `getScoreHistory()` في `server/src/repo/scoresRepo.ts` (سطر 59) - **معرَّفة لكن غير مُستدعاة من أي مكان في كامل المستودع** (تأكَّد بالبحث الشامل - صفر استخدام خارج تعريفها).
  - `apiUsage` collection كاملة - تُقرَأ لكن لا تُكتَب بعد اليوم (راجع خطأ #4).
- **TODOs:** **صفر** - لم يُعثَر على أي `TODO`/`FIXME`/`HACK`/`XXX` في كامل قاعدة الكود (بحث شامل بـ grep).
- **ملفات تحتاج إعادة هيكلة:** لا يوجد ملف واحد يستدعي إعادة هيكلة عاجلة بناءً على الحجم/التعقيد المفحوص الآن.

---

## 10. Architecture Review

### شجرة المشروع (المستوى الأول والثاني)

```
Sahmi/
├── .github/workflows/scheduled-scans.yml   # الجدولة (GitHub Actions cron)
├── firestore.rules                         # قواعد الأمان
├── firestore.indexes.json
├── render.yaml                             # إعداد نشر Render (IaC)
├── firebase.json                           # إعداد Firebase Hosting فقط (لا Functions)
├── server/                                 # Express Backend (Render)
│   └── src/
│       ├── alerts/          (2 ملفات: alertTier, evaluateAlert)
│       ├── config/          (2: env, firebaseAdmin)
│       ├── jobs/             (9: refreshQuotes/Historical/Financials/Dividends/Companies، computeScores، runAlertCheck، scanAndAlertAll، scanTargets)
│       ├── middleware/       (3: auth، cronAuth، errorHandler)
│       ├── repo/              (12: طبقة الوصول لـ Firestore لكل مجموعة)
│       ├── routes/            (2: api.ts (محمي بمصادقة مستخدم)، internal.ts (محمي بسر مشترك للجدولة))
│       ├── scheduled/          (3: الوظائف المجدولة الثلاث)
│       ├── scoring/            (8: محرك التقييم الكامل)
│       ├── services/sahmk/      (8: تكامل SAHMK API)
│       ├── services/telegram/   (2: تكامل Telegram)
│       ├── technical/           (5: RSI/SMA/مستويات الدعم)
│       └── utils/                (7: batchRunner، jobLock، logger، rateLimit...)
└── frontend/                                # React + Vite (Firebase Hosting)
    └── src/
        ├── components/  (5: AppShell، PriceChart، ProtectedRoute، ScoreBadge، StatCard)
        ├── hooks/        (2: useAuth، useCollection)
        ├── lib/           (2: firebase.ts، backend.ts)
        └── pages/          (8: Login، Dashboard، Explorer، StockDetail، Watchlist، DividendCalendar، Settings، DevTest)
```

### تدفّق البيانات الفعلي (مؤكَّد بالكود)

```
   SAHMK API (7 endpoints حقيقية)
        │  (سيرفر Render فقط - المفتاح لا يظهر للعميل إطلاقًا)
        ▼
   server/src/services/sahmk/SahmkService.ts + httpClient.ts
        │  (retry + احترام 429 + تسجيل استخدام بالذاكرة)
        ▼
   server/src/jobs/*  (refreshQuotes/Historical/FinancialsAndRatios/Dividends/Companies)
        │
        ▼
   Firestore (عبر server/src/repo/*)  ←── هذه هي "Firebase" في السؤال المطروح (لا توجد Cloud Functions وسيطة)
        │
        ├──► server/src/scoring/*  (يُحسَب عند الطلب من runAlertCheck/computeScores، ثم يُكتَب في scores/)
        │
        ▼
   server/src/alerts/evaluateAlert.ts  (9 شروط)
        │
        ▼ (فقط لو اجتازت كل الشروط)
   server/src/services/telegram/TelegramService.ts → Telegram Bot API
        │
        ▼ (بالتوازي، مستقل عن Telegram)
   Firestore (scores, alerts) ──► frontend/src/hooks/useCollection.ts (onSnapshot مباشر، بدون route وسيط)
        │
        ▼
   Dashboard / Explorer / StockDetail / DividendCalendar (React، قراءة مباشرة من Firestore عبر Firebase SDK بمصادقة المستخدم)
```

**ملاحظة معمارية مهمة:** الواجهة **لا تمر عبر السيرفر (Render) للقراءة** - تقرأ من Firestore مباشرة عبر Firebase SDK بمصادقة المستخدم (`onSnapshot`). السيرفر (Render) يُستخدَم فقط للكتابة (المهام المجدولة) وبعض العمليات الخاصة (اختبار Telegram، تحديث الإعدادات، فحص تنبيه يدوي). هذا تصميم سليم ويقلّل الحمل على Render.

---

## 11. Build Health

| المقياس | العدد |
|---|---:|
| ملفات السيرفر (`.ts`, بدون اختبارات) | 65 |
| ملفات اختبار السيرفر (`.test.ts`) | 6 (تحتوي 29 اختبارًا) |
| ملفات الواجهة (`.ts`/`.tsx`) | 21 |
| Components (الواجهة) | 5 |
| Pages (الواجهة) | 8 |
| Hooks | 2 |
| Services (سيرفر) | 2 مجلدين (`sahmk/` 8 ملفات، `telegram/` 2 ملف) |
| Repos (طبقة Firestore) | 12 |
| Jobs (مهام قابلة للاستدعاء) | 9 |
| Scheduled Jobs (مجدولة فعليًا) | 3 |
| Cloud Functions | **0** (لا توجد إطلاقًا) |
| Firestore Collections (مستخدَمة في الكود) | 15 + subcollection واحدة |
| TypeScript Errors (سيرفر) | **0** |
| TypeScript Errors (واجهة) | **0** |
| ESLint Errors (سيرفر) | **0** |
| ESLint Errors/Warnings (واجهة) | 0 أخطاء / 1 تحذير (`react-refresh` في `useAuth.tsx`) |
| Runtime Errors (مؤكَّدة فعليًا في الإنتاج، آخر 48 ساعة) | **2 نوع مختلف، تكرّرا عدة مرات**: `RESOURCE_EXHAUSTED` (مرتين)، قفل `busy` بلا نهاية (مرة، غير محلولة) |
| نسبة نجاح آخر 24 تشغيلة GitHub Actions | 14 نجاح / 24 (~58%) |

---

## 12. Final Assessment

### التقييم العام: **58 / 100**

هذا الرقم يعكس فجوة حقيقية بين جودة الهندسة الأساسية (محرك التقييم، بنية الكود، الواجهة) وموثوقية التشغيل الفعلي (تسليم التنبيهات في وقتها، بدون انقطاع).

### ما الذي يمنع المشروع من أن يكون جاهزًا للاستخدام الفعلي؟

الوعد الأساسي للمشروع هو: **"احسب تقييمًا دقيقًا لكل سهم في السوق، وأرسل لي تنبيهًا فوريًا حين يستوفي سهم الشروط."** الجزء الأول (الحساب) مؤكَّد أنه يعمل. الجزء الثاني (التنبيه في وقته، بثقة) **لم يُثبَت فعليًا حتى الآن** - لا يوجد يوم واحد موثَّق اكتملت فيه دورة الفحص الكاملة بنجاح 100% خلال آخر 3 أيام، ولا يوجد دليل واحد على وصول رسالة Telegram حقيقية.

### أعلى 10 مشاكل يجب إصلاحها أولًا (بدون تنفيذ الآن، بناءً على طلبك):

1. إضافة hard timeout لقفل `jobLock` حتى لا يعلق للأبد (خطأ #1).
2. حسم قرار خطة Firebase (Blaze أو تقليل حجم أي اختبار مستقبلي) لوقف تكرار استنزاف الحصة (خطأ #2).
3. إيجاد بديل/تعويض لعدم موثوقية جدولة GitHub Actions (مثلًا: مراقبة خارجية تتحقق من "آخر تشغيل ناجح" وتنبّه لو تأخر) (خطأ #3).
4. إصلاح أو حذف مؤشر "استهلاك API" المعطوب في Dashboard (خطأ #4).
5. **تأكيد فعلي واحد على الأقل**: تشغيل يدوي مُتحكَّم به لسهم حقيقي (أو بيانات اختبار مصمَّمة خصيصًا) يجتاز كل الشروط التسعة، والتأكد بصريًا من وصول رسالة Telegram حقيقية (خطأ #5).
6. تحديث تعليق `firestore.rules` القديم (خطأ #6).
7. بناء واجهة بسيطة لعرض `syncJobs` (سجل الأحداث) بما أن البيانات جاهزة أصلًا (خطأ #7).
8. حذف الكود الميت (`getScoreHistory` غير المُستخدَمة، أو ربطها فعليًا بواجهة).
9. توثيق/تأكيد القيم الفعلية الحية لمتغيرات البيئة على Render (Telegram، SAHMK، إلخ) - لم تُفحَص مباشرة.
10. تقسيم حزمة الواجهة الأمامية (code splitting) لتقليل حجم الـbundle.

### ما الذي يمكن تأجيله:

- Risk Score، AI Summary، Opportunity History، Scheduled Reports، Export Excel/PDF، Multi-Filter Presets - كلها ميزات إضافية حقيقية غير موجودة، لكن لا شيء منها يمنع الوظيفة الأساسية (تقييم + تنبيه) من العمل.
- تحسين "سجل تغيّر التقييم" ليعرض تفاصيل كل فحص بدل رقم إجمالي فقط.
- Confidence Score كمفهوم مستقل (البيانات موجودة، فقط يحتاج عرضًا/تسمية مختلفة).

### ما الذي يُعتبَر جاهزًا للإنتاج بالفعل:

- محرك التقييم (Investment Score) بكل فئاته الأربع - مختبَر ومؤكَّد.
- استبعاد القطاع المالي - مؤكَّد فعليًا.
- Build كامل (سيرفر + واجهة) - نظيف تمامًا، صفر أخطاء TypeScript/ESLint.
- الواجهة الأساسية (Dashboard، Explorer، DividendCalendar، StockDetail، Settings، Watchlist) - تعمل، مبنية جيدًا، ومطابقة لهوية RTL عربية متسقة.
- تكامل SAHMK - موثوق، يحترم Rate Limiting فعليًا، بدون بيانات ملفَّقة.
- أوزان التقييم المخصصة (Custom Scoring Weights) - كاملة وتعمل من الواجهة للباك إند.
